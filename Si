WebElement elemento = driver.findElement(By.cssSelector(".dav-section__content"));

((JavascriptExecutor) driver).executeScript(
    "arguments[0].scrollIntoView({block: 'center'});",
    elemento
);
