public void presionarMultiplesTabs(WebDriver driver, WebElement elemento, int cantidad) {

    Actions actions = new Actions(driver);

    actions.moveToElement(elemento).click();

    for (int i = 0; i < cantidad; i++) {
        actions.sendKeys(Keys.TAB);
    }

    actions.perform();
}
