public interface EHLLAPI extends Library {

    EHLLAPI INSTANCE =
        Native.load("pcshll32", EHLLAPI.class);

    void hllapi(
            IntByReference function,
            char[] data,
            IntByReference length,
            IntByReference returnCode
    );

}
