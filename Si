import com.fasterxml.jackson.databind.ObjectMapper;
import java.nio.file.Files;
import java.nio.file.Paths;
import java.util.HashMap;
import java.util.Map;

public class EnviarCodigo {

    public static void main(String[] args) {
        try {
            // Leer archivo txt con tu código
            String contenido = new String(Files.readAllBytes(Paths.get("C:/ruta/codigo.txt")));

            // Crear estructura JSON
            Map<String, String> data = new HashMap<>();
            data.put("mensaje", contenido);

            // Convertir a JSON válido (ESCAPA TODO AUTOMÁTICO)
            ObjectMapper mapper = new ObjectMapper();
            String json = mapper.writeValueAsString(data);

            // Imprimir (esto ya no rompe nada)
            System.out.println(json);

        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}
